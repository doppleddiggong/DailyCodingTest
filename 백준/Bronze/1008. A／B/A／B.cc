#include <cstdio>
#include <iostream>

using namespace std;

int main()
{
    double a,b;
    
    cin >> a >> b;
    double f = a/b;
    cout.precision(10);
    cout << f << endl;
    return 0;
}