#include <cstdio>
#include <iostream>

using namespace std;

int main()
{
    long long price;

    cin >> price;

    cout << (long long)(price * 0.78) << " " << (long long)(price * 0.956) << endl;
    return 0;
}